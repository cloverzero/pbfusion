import { SiteHeader } from "@/components/page-header";

export default function Homepage() {
  return (
    <main>
      <SiteHeader />
      <div>
        <h1>Homepage</h1>
      </div>
    </main>
  );
}
